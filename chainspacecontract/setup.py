from setuptools import setup


setup(
    name = 'chainspacecontract',
    version = '0.1',
    packages = ['chainspacecontract'],
    install_requires = [
        'flask',
        'requests',
    ],
)
